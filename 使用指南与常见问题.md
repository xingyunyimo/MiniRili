# MiniRili 使用指南与常见问题

## 一、快速上手

### 安装 App
1. 从 GitHub Releases 页面下载最新版 APK
2. 在手机上打开 APK 文件安装
3. 首次打开会显示隐私政策，阅读后点击"同意"进入应用

### 界面导航

应用以日历页为主界面，功能入口都在日历页顶栏：

- **视图切换 / 导入导出** — 右上角"日历"图标下拉菜单
- **搜索事件** — 顶栏搜索图标（按标题 / 描述模糊匹配）
- **所有事件** — 顶栏列表图标，进入全部事件列表页
- **回到今天** — 顶栏"今天"图标
- **新建事件** — 右下角"+"悬浮按钮
- **天气** — 日历页内的天气卡片，点击进入天气详情页

---

## 二、功能详解

### 📅 日历视图

下拉菜单可切换三种视图（周视图代码已内置，暂未开放入口）：

| 视图 | 说明 | 推荐用途 |
|---|---|---|
| 月视图 | 默认视图，每格显示公历日 + 农历/节气/节假日小字，最多列出 2 条事件标题（超出显示 `·N`），事件带颜色条 | 概览整月 |
| 日视图 | 顶部当日信息卡（公历+星期+农历+节假日）+ 天气卡 + 当日事件列表（重复事件也会展开显示） | 查看某天详细安排 |
| 年视图 | 12 个月缩略排列，点击某日进入日视图，点击月份标题跳转到月视图 | 快速定位月份 |

**操作：**
- **翻页** — 左右滑动切换上/下月（年视图切换上/下一年）
- **跳转** — 点击顶栏日期弹出年月日选择器，快速跳转到任意日期
- **回到今天** — 点击顶栏"今天"图标
- **双击日期** — 在月视图中双击某天，切换到该天的日视图

### 📝 事件与提醒

**新建事件：**
1. 点击右下角"+"按钮，或月视图中点击某天的空白区域
2. 填写标题（必填，为空时保存会提示）、描述、选择日期和时间
3. 选择日期类型：阳历 / 农历（农历模式下日期选择器直接填农历日期，支持闰月）
4. 设置提醒提前量：当日 / 提前 5 分钟 / 15 分钟 / 30 分钟 / 1 小时 / 1 天
5. 选择通知方式：**默认仅开启通知栏推送**，闹钟响铃需手动开启
6. 可选：全天开关、重复类型、类型、标签、颜色、优先级、附件

**关于全天事件：**
- 开启"全天"后仍可开启提醒，触发时刻固定为**当天 09:00**
- 若提醒提前量导致触发时刻已经过去（如"提前 1 天"提醒今天的事件），保存时会弹窗确认，避免设置了却收不到提醒的静默失效

**重复事件：**
- 重复类型：每日 / 每周 / 每月 / 每年 / 工作日 / 周末
- 农历模式可选：每月（农历）/ 每年（农历），如农历生日每年自动重复
- **重复事件在日历视图上会展示在所有重复发生的日期上**，不只是创建日期
- 重复事件可单独跳过某次：
  - 详情页"跳过当天的提醒"按钮 — 仅当天不响，事件仍显示在日历上，可随时"恢复提醒"
  - 删除时选"仅删除本次" — 当天既不显示也不提醒

**删除事件：**
- 非重复事件：直接删除，不可恢复
- **重复事件删除时提供两个选项**：
  - **仅删除本次** — 只删除选中日期的这次出现，其他日期不受影响
  - **删除全部** — 删除所有重复出现的事件，不可恢复

**提醒延后（Snooze）：**
- 通知栏自带"延后 5 分钟 / 1 小时 / 1 天"三个按钮，可反复延后

**查看/编辑事件：**
- 在日历上点击事件条目 → 进入事件详情页
- 详情页可编辑事件内容、标记完成、上下移动调整排序（↑↓ 按钮）
- 附件支持点击打开，也可单独删除

**事件支持的功能：**
- 优先级标记（低 / 中 / 高）
- 完成状态标记（详情页勾选，日历视图仍会展示已完成事件）
- 标签分类与颜色标记
- 附件添加

### 🌤️ 天气

日历页内嵌天气卡片（今日概览），点击进入天气详情页，包含：
- **当前天气** — 温度、天气图标、天气描述
- **24 小时预报** — 逐小时气温+天气
- **15 天趋势** — 每日最高/最低气温+天气（数据不足时回退今日）
- **日出日落** — 当日及次日日出日落时间
- **出行建议** — 基于天气规则引擎生成的场景化提醒（高温/降雨/大风/AQI 等）
- **空气质量（AQI）** — PM2.5 / PM10 / O3 / NO2 / CO / SO2 分项及等级
- **生活指数** — 紫外线、穿衣、洗车、运动、感冒、洗晒 6 项建议

**城市管理：**
- 天气页顶部显示当前城市，多城市以标签行排列
- 点击"添加城市"搜索并添加关注的城市（内置全国县级及以上约 3320 条行政区数据，离线可搜）
- 点击城市标签切换；滑动到最左端可回到定位城市
- 支持删除城市；GPS 定位获取当前位置天气（每天 0 点后首次进入自动刷新一次定位城市）

**定位权限：**
- 首次进入天气页时，若未授权定位且未添加城市，会显示引导卡片
- 点击"允许定位"按钮，授予位置权限
- 也可以手动搜索城市添加，无需定位权限

### 🔔 每日天气通知

应用支持每日定时推送天气通知：
- 默认推送时间：每日 08:00
- 推送内容：今日天气概况 + 出行建议
- 在天气页的"出行建议"卡片右上角齿轮按钮中可配置：
  - 开关：启用/关闭每日通知
  - 时间：选择推送的小时和分钟

### 🏷️ 节假日

- 中国法定节假日以红色标记在日历上显示
- 调休补班日以特殊标记显示
- 数据基于国务院发布的安排，内置 2021–2026 年数据
- 节假日信息无需联网，离线可用

### 💾 数据导入导出

**导出（仅 ICS）：**
1. 在日历页点击右上角菜单 → 导出 ICS
2. 选择保存位置
3. 所有事件（含已完成）将导出为一个 `.ics` 文件

**导入（ICS / JSON 均可）：**
1. 在日历页点击右上角菜单 → 导入 ICS
2. 在文件浏览器中选择 `.ics` 或 `.json` 文件
3. 事件自动导入，重复事件（标题+日期+时间+描述相同）自动去重

**格式说明：**
- ICS：标准日历交换格式（RFC 5545），可导入 Google Calendar、Outlook 等其他日历应用
- JSON：MiniRili 专属格式。JSON 文件可作为导入来源，但应用内导出仅提供 ICS
- MiniRili 的农历、重复规则等扩展信息以 `X-MINIRILI-*` 私有属性写入 ICS；其他日历应用读取时会退化为纯公历事件

### 📱 桌面小部件

应用提供了一种桌面小部件：
- **4×2 组合小部件**：同时显示公历/农历/节气信息、今日天气、今日事件列表（含重复事件展开）；多事件时自动轮播

**添加方法：**
Android 桌面长按空白处 → 小部件 → 找到 MiniRili → 添加组合小部件。

---

## 三、权限说明

| 权限 | 用途 | 是否必须 |
|---|---|---|
| 通知（POST_NOTIFICATIONS） | 发送事件提醒和每日天气通知 | 推荐开启 |
| 位置（ACCESS_FINE_LOCATION / ACCESS_COARSE_LOCATION） | 获取当前位置天气 | 可选 |
| 精确闹钟（SCHEDULE_EXACT_ALARM） | 确保提醒准时触发 | 推荐开启 |
| 开机自启动 / 后台运行（厂商白名单） | 保证重启后提醒恢复、闹钟不被杀 | 强烈建议开启 |

首次启动会引导开启通知权限与厂商自启动白名单；自启动引导确认后 30 天内不再重复弹出。

---

## 四、常见问题

### Q1：收不到事件提醒？

**可能原因及解决方法：**

1. **通知权限未开启**（最常见）
   - 前往手机"设置" → "应用管理" → "MiniRili" → "通知管理"
   - 确保通知开关已打开
   - 首次安装时应用会弹出权限请求，请选择"允许"

2. **精确闹钟权限未开启**（Android 12+）
   - 前往"设置" → "应用管理" → "MiniRili" → "特殊权限" → "闹钟和提醒"
   - 确保已允许

3. **手机重启后未打开应用**
   - 重启手机后，应用会通过开机自启广播自动恢复已注册的提醒
   - 前提是已授予开机自启动/后台白名单（见"权限说明"）；若未授予，请手动打开一次应用

4. **提醒触发时刻已经过去**
   - 触发时刻 = 事件日期时间 − 提前量。若该时刻早于保存时间，提醒不会触发（保存时应用会弹窗提醒）

5. **当天提醒被跳过**
   - 重复事件详情页若显示红色"当天提醒已跳过"提示，点击"恢复提醒"即可

### Q2：天气数据不显示或显示异常？

1. **检查网络连接** — 天气数据需要联网获取
2. **定位未开启** — 如果使用当前位置，确保定位权限已授予
3. **手动添加城市** — 也可以点击"添加城市"搜索城市手动添加
4. **数据缓存** — 天气数据缓存 30 分钟，不会每次打开都刷新
5. **15天限制** — Open-Meteo 免费 API 最多提供 15 天预报，超出范围会回退到今日数据

### Q3：日期显示不对或农历显示异常？

- 应用基于 Android 内置的 ICU4J ChineseCalendar，覆盖 1900–2200 年
- 如果系统日期设置异常，请检查手机"设置" → "日期和时间"是否为自动

### Q3b：农历事件设置了时间，保存后变成全天？

**原因**：农历日期（如"己亥年六月初九"）对应的公历日期在 1959 年，早于 1970 年 Unix 纪元起点，旧版本中时间戳计算为负数，被误判为"全天"。

**解决方法**：升级到最新版本。如果已保存的事件仍显示为全天，请编辑该事件，重新设置时间后保存即可。

### Q3c：以前新建的农历事件日期偏了（如农历七月初九变成八月廿二）？

**原因**：旧版本编辑弹窗存在缺陷——重新打开农历事件时把公历日期预填进了农历输入框，再保存时公历数字被当作农历日期反推，导致日期漂移（例如 1953 农历七月十三 / 阳历 8-22 被改成 农历八月廿二 / 阳历 9-29）。

**解决方法**：最新版本已修复（编辑时按农历值预填，支持闰月）。历史被漂移的事件需手动纠正：打开事件详情 → 修改日期为正确的农历日期 → 保存。新版保存后日期不会再漂移。

### Q4：导入 ICS 文件失败？

1. 确保文件编码为 UTF-8
2. 确保文件符合 RFC 5545 标准格式
3. 导入大文件时请耐心等待
4. 如果仍然失败，可以尝试导出 JSON 再导入

### Q5：可以备份事件数据吗？

可以。使用导出功能（ICS）将数据保存到手机存储，之后可以通过导入（ICS / JSON）恢复。
建议定期备份，特别是在系统重置或换手机之前。

### Q6：应用需要哪些权限？安全吗？

应用**不需要任何账户**，**不需要联网注册**，**不上传任何个人数据**。
所有数据（事件、设置）仅存储在本地设备上。
天气数据通过直接调用 Open-Meteo 公共 API 获取，不经过第三方服务器中转。

### Q7：如何删除所有事件？

目前没有一键清空功能，需要在"所有事件"列表中逐条删除，或通过导入空文件覆盖。

### Q8：桌面小部件不更新？

- 确保应用没有被系统杀死
- 部分厂商对小部件刷新频率有限制
- 可以尝试移除小部件后重新添加
- 天气部分每 30 分钟自动刷新一次；日期变更后 0 点自动刷新

### Q9：为什么是 debug 版本？

当前 Release 提供的是 debug 构建的 APK，安装时系统会提示"未知来源"或"此应用为调试版本"。这不影响正常使用。

后续版本会提供 release 签名版本消除此提示。

### Q9b：全天事件能提醒吗？

可以。全天事件开启通知后，触发时刻固定为**当天 09:00**（提前量照常生效，如选"1 天前"则前一天 09:00 提醒）。

### Q10：应用支持平板吗？

应用未针对平板做专门适配，但在平板上可以正常运行。建议在竖屏模式下使用。

---

## 五、隐私说明

- MiniRili **不收集任何用户数据**
- 所有事件数据仅存储在本地设备 Room 数据库中
- 天气数据请求直接发送到 Open-Meteo API，不经任何中间服务器
- 应用无需创建账户、无需登录
- 详细隐私政策见应用内"隐私政策"页面

---

## 六、反馈与建议

- 如果遇到问题，请在 GitHub 提交 [Issue](https://github.com/xingyunyimo/MiniRili/issues)
- 功能建议或使用讨论，欢迎在 [Discussions](https://github.com/xingyunyimo/MiniRili/discussions) 发帖
- 如果你喜欢这个应用，给仓库点个 ⭐ 就是最大的支持
